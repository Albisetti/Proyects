export const verticalNavigationQuery = `_type == 'verticalNav' => {
    _type,
    _key,
    skyParkLabel,
    floorLabel,
    amenitiesLabel,
    buildingLabel,
    locationLabel,
    teamLabel,
  }`;