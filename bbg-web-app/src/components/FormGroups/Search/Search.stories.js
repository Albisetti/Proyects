import React from 'react';
import Search from './Search';

export default {
    title: 'BBG/FormGroup/Search',
    component: Search,
    argTypes: {
        
    }
}

const Template = (args) => <Search {...args} />

export const Default = Template.bind({})
Default.args = {

}
