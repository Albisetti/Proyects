import React from 'react';
import RadioGroup from './RadioGroup';

export default {
    title: 'BBG/Components/RadioGroup',
    component: RadioGroup,
    argTypes: {
        
    }
}

const Template = (args) => <RadioGroup {...args} />

export const Default = Template.bind({})
Default.args = {
    
}
