import React from 'react';
import RichText from './RichText';


export default {
  title: 'BBG/Components/RichText',
  component: RichText,
  controls: {
    
  }
}

const Template = () => <RichText />

export const Default = Template.bind({})
Default.parameters = {
    
}