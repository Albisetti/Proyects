type claimPeriod {
    error: String
    year: Int
    quarter: Int
    report_period: String
    uncloseBuilders: [Organizations]
    closeBuilders: [closeBuildersPeriod]
}

type closeBuildersPeriod {
    builder:Organizations
    total:calculateClaimTotal
}
